<div class="lab-mark" aria-hidden="true">
  <span class="lab-mark__glow"></span>
  <span class="lab-mark__orbit lab-mark__orbit--outer"></span>
  <span class="lab-mark__orbit lab-mark__orbit--middle"></span>
  <span class="lab-mark__orbit lab-mark__orbit--inner"></span>
  <span class="lab-mark__point lab-mark__point--one"></span>
  <span class="lab-mark__point lab-mark__point--two"></span>
  <span class="lab-mark__point lab-mark__point--three"></span>
  <span class="lab-mark__point lab-mark__point--four"></span>
  <span class="lab-mark__core"><strong>WR</strong><small>Labs</small></span>
</div>

<style>
  .lab-mark {
    position: relative;
    width: min(100%, 26rem);
    aspect-ratio: 1;
    margin-inline: auto;
  }

  .lab-mark__glow,
  .lab-mark__orbit,
  .lab-mark__point,
  .lab-mark__core {
    position: absolute;
  }

  .lab-mark__glow {
    inset: 18%;
    border-radius: 50%;
    background: rgb(105 86 232 / 12%);
    filter: blur(2rem);
  }

  .lab-mark__orbit {
    border: 1px solid var(--border-strong);
    border-radius: 50%;
  }

  .lab-mark__orbit--outer { inset: 4%; }
  .lab-mark__orbit--middle { inset: 20%; border-style: dashed; }
  .lab-mark__orbit--inner { inset: 35%; }

  .lab-mark__point {
    width: 0.75rem;
    height: 0.75rem;
    border: 0.2rem solid var(--background);
    border-radius: 50%;
    background: var(--primary);
    box-shadow: 0 0 0 1px var(--primary-border);
  }

  .lab-mark__point--one { top: 2.5%; left: calc(50% - 0.375rem); }
  .lab-mark__point--two { top: 32%; right: 11%; background: var(--info); }
  .lab-mark__point--three { bottom: 12%; left: 18%; background: var(--warning); }
  .lab-mark__point--four { top: calc(50% - 0.375rem); left: 19%; background: var(--success); }

  .lab-mark__core {
    inset: 38%;
    display: grid;
    align-content: center;
    justify-items: center;
    border: 1px solid rgb(255 255 255 / 10%);
    border-radius: 50%;
    background: var(--dark-section);
    color: var(--dark-text);
    box-shadow: 0 1rem 3rem rgb(17 19 27 / 20%);
    line-height: 1;
  }

  .lab-mark__core strong { font-size: clamp(0.9rem, 2vw, 1.2rem); letter-spacing: -0.08em; }
  .lab-mark__core small { margin-top: var(--space-1); color: var(--dark-muted); font-size: 0.55rem; }

  @media (max-width: 43.999rem) {
    .lab-mark { width: min(78vw, 19rem); }
  }
</style>
